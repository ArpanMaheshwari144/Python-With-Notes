mySet1 = {8, 7, 12, "Arpan", [1,2]} # we cannot add list into the set bcoz list can change but set cannot be changed
print(mySet1)

# set cannot be changed
# cannot allows list inside the set
# tuple cannot be changed
